import { ServerImgPipePipe } from './server-img-pipe.pipe';

describe('ServerImgPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ServerImgPipePipe();
    expect(pipe).toBeTruthy();
  });
});
